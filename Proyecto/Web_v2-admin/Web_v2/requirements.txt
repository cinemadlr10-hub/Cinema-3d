# Core web stack
Flask>=3.0,<4.0
Jinja2>=3.1
Werkzeug>=3.0

# Email
Flask-Mail>=0.9.1,<1.0

# Variables de entorno (.env)
python-dotenv>=1.0

# PDFs & QR
fpdf2>=2.7
qrcode[pil]>=7.4

# --- Opcionales (descomenta si los usás) ---
# CSRF/forms
# Flask-WTF>=1.2

# Rate limiting
# Flask-Limiter>=3.7

# Servidores de producción (elige uno según tu entorno)
# gunicorn>=21.2     ; platform_system != "Windows"
# waitress>=2.1      ; platform_system == "Windows"
